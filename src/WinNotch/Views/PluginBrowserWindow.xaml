<Window x:Class="WinNotch.Views.PluginBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Browse Plugins"
        Width="900"
        Height="680"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Components/PluginSurfaceStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DropShadowEffect x:Key="PluginWindowShadow"
                              BlurRadius="34"
                              ShadowDepth="0"
                              Opacity="0.24"
                              Color="Black"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border Margin="18"
                Padding="24"
                Background="{DynamicResource NotchFillBrush}"
                CornerRadius="30"
                Effect="{StaticResource PluginWindowShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,18" MouseLeftButtonDown="OnHeaderMouseDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Browse Plugins"
                                   FontSize="28"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="Discover verified widgets and utilities that fit directly into WinNotch."
                                   Margin="0,6,0,0"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>

                    <Button x:Name="CloseButton"
                            Grid.Column="1"
                            Width="32"
                            Height="32"
                            Content="X"
                            Style="{StaticResource PluginTitlebarButton}"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBox"
                             Height="42"
                             VerticalContentAlignment="Center"
                             Style="{StaticResource PluginSearchTextBox}"/>

                    <TextBlock x:Name="ResultsText"
                               Grid.Column="1"
                               Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Foreground="{DynamicResource TextMutedBrush}"/>

                    <Button x:Name="SearchButton"
                            Grid.Column="2"
                            MinWidth="96"
                            Margin="14,0,0,0"
                            Content="Search"
                            Style="{StaticResource PluginPrimaryButton}"/>
                </Grid>

                <Border Grid.Row="2"
                        Background="{DynamicResource SegmentedBgBrush}"
                        CornerRadius="26"
                        Padding="18,18,10,18">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Style="{StaticResource RoundedScrollViewer}">
                        <Grid>
                            <WrapPanel x:Name="PluginGridPanel"
                                       ItemWidth="256"
                                       Orientation="Horizontal"/>

                            <StackPanel x:Name="EmptyStatePanel"
                                        Margin="12,24,12,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Top"
                                        Visibility="Collapsed">
                                <TextBlock x:Name="EmptyStateTitleText"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="EmptyStateBodyText"
                                           Margin="0,8,0,0"
                                           MaxWidth="520"
                                           FontSize="12"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
