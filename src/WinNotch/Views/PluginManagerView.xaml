<UserControl x:Class="WinNotch.Views.PluginManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Plugin Manager" FontSize="20" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Extend WinNotch with custom plugins" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Plugin List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,0,20,10">
            <StackPanel x:Name="PluginListPanel">
                <!-- Plugins will be added here dynamically -->
            </StackPanel>
        </ScrollViewer>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <Button x:Name="RefreshLibraryButton" Content="Refresh Library" Width="120" Height="32" Margin="0,0,10,0"/>
            <Button x:Name="BrowsePluginsButton" Content="Browse Plugins" Width="120" Height="32" Margin="0,0,10,0"/>
            <Button x:Name="OpenPluginsFolderButton" Content="Open Folder" Width="110" Height="32"/>
        </StackPanel>
    </Grid>
</UserControl>
