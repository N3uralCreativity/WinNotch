<UserControl x:Class="WinNotch.Views.ClockWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Compact clock shown in closed notch when no music is playing -->
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock x:Name="TimeText"
                   Text="12:00"
                   Foreground="#CCCCCC"
                   FontSize="11"
                   FontWeight="SemiBold"
                   VerticalAlignment="Center"/>
        <TextBlock Text="·"
                   Foreground="#555555"
                   FontSize="11"
                   VerticalAlignment="Center"
                   Margin="5,0,5,0"/>
        <TextBlock x:Name="DateText"
                   Text="Thu 17"
                   Foreground="#888888"
                   FontSize="10"
                   VerticalAlignment="Center"/>
    </StackPanel>
</UserControl>
